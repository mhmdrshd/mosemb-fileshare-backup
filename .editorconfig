# shfmt (and most editors) read this; without it shfmt assumes tab indentation
# and `shfmt -d .` flags every file in the repo.
root = true

[*]
end_of_line = lf
insert_final_newline = true
charset = utf-8

[*.{sh,example,bats}]
indent_style = space
indent_size = 4
switch_case_indent = true
